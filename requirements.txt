# Tested with Python 3.12. Version floors are the versions this repo was
# validated against; newer minor releases are expected to work.
numpy>=1.24,<3
scipy>=1.10
sympy>=1.11
matplotlib>=3.6
pytest>=7.0
